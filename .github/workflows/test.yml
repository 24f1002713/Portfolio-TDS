name: Email Verification Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f1002713@ds.study.iitm.ac.in
        run: echo "Hello, world!"
